{
  "name": "omarchy-twingate",
  "private": true,
  "description": "Test harness for the Omarchy Twingate plugin. The plugin itself is QML and needs no build step.",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
